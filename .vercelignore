# Exclude local environment and cache
.venv/
node_modules/
__pycache__/
*.pyc
.git/

# Exclude large data files not needed in cloud
dataset_json/
chat_history.db
MCP/

# Exclude development scripts
check_rag_scores.py
check_rag_full.py
automate.py
test_*.py

# DO NOT ignore frontend/ here, because Vercel needs it to run the build command!
# We will use vercel.json to keep the serverless function small instead.
